apiVersion: apps/v1
kind: Deployment
metadata:
  name: authentication
spec:
  replicas: 1
  selector:
    matchLabels:
      app: authentication
  template:
    metadata:
      labels:
        app: authentication
    spec:
      containers:
        - name: authentication
          image: pis6toregistrygroup.azurecr.io/authentication:1.0
          ports:
            - containerPort: 4001
      imagePullSecrets:
        - name: my-acr-secret
